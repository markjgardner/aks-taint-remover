on:
  push:
    branches:
    - main
    # for workflow dev
    - github
jobs:
  build-linux-image:
    uses: markjgardner/aks-taint-remover/.github/workflows/template.yaml@github
    with:
      baseTag: "6.0-alpine"
      runner: ubuntu-latest
 